# دليل الاستخدام | مجمع الأخبار السوري والنشر الذكي للووردبريس 📰🤖

يرحب بك هذا الدليل المتكامل في **مجمع الأخبار السورية الذكي** و**ناشر الووردبريس التلقائي**. لقد تم تصميم هذا النظام البرمجي المتكامل ليعمل بشكل مستقل أو تفاعلي كلياً لمساعدتك على جلب أحدث الأخبار من أكثر من **35 موقعاً إخبارياً سورياً وعربياً** على مدار الساعة، وإعادة صياغتها بالذكاء الاصطناعي لمنع الحقوق وتكرار المحتوى، ونشرها فورياً كمسودات أو مقالات منشورة بموقع الووردبريس الخاص بك.

---

## 🌟 مميزات النظام الرئيسية

1. **جلب متعدد القنوات**: يدعم النظام 3 استراتيجيات متطورة لجلب الأخبار:
   - **WordPress REST API (wp_api)**: جلب ذكي، فائق السرعة، وموفر للموارد للمواقع المبنية على ووردبريس.
   - **RSS XML Feeds (rss)**: استخدام المغذيات القياسية والمستقرة.
   - **HTML Scraper (html)**: مستخرج زحف متطور باستخدام Cheerio لقراءة وتحليل وتفريغ محتوى الصفحات مباشرة عند عدم توفر RSS.
2. **منع التكرار الذكي بنسبة 100%**:
   - التحقق الفوري من تطابق الروابط `URL`.
   - خوارزمية **Jaccard Distance** للتحقق من نسبة تطابق النصوص والعناوين لمنع تكرار نشر الخبر في حال نشره بصيغة مشابهة في موقع آخر (عتبة التطابق الافتراضية: 85%).
3. **إعادة صياغة ذكية بالذكاء الاصطناعي**:
   - تكامل كامل مع **OpenAI Chat Completions API**.
   - دعم للنماذج فائقة الكفاءة مثل `gpt-4o-mini` لإنتاج صياغة صحفية احترافية باللغة العربية الفصحى مع الاحتفاظ بكود التنسيق الأصلي للمقال (وسوم HTML).
4. **لوحة تحكم تفاعلية مذهلة**:
   - واجهة مستخدم مظلمة تعتمد على تقنية **Glassmorphism (التأثيرات الزجاجية الشفافة)**.
   - نظام فحص اتصال حي ومعتمد للمنافذ الخارجية (WordPress & OpenAI).
   - منفذ أوامر مباشر **Console Terminal** تفاعلي يعرض سجلات الدمج وسحب الأخبار لحظة بلحظة عبر Server-Sent Events (SSE).
   - سجل استعراض تفصيلي ومبهر للمقالات المؤرشفة مع خيارات التصفية والبحث المتقدم بالصفحات.

---

## 🛠️ متطلبات التشغيل

- **Node.js**: الإصدار v18.0.0 أو أحدث (الإصدار المستحسن: v20+).
- **SQLite3**: يتم إنشاؤه وتهيئته تلقائياً بواسطة الخادم.
- **اتصال بالإنترنت** لجلب الأخبار والربط مع OpenAI وووردبريس.

---

## 📂 الهيكل الداخلي للمجلد ومكونات النظام

تحتوي الحزمة البرمجية داخل المجلد `syrian_news_aggregator/` على الملفات التالية:
- `index.js`: البوابة والمنسق الرئيسي (يدعم التشغيل الصامت `--cron` أو تشغيل لوحة التحكم).
- `server.js`: خادم لوحة التحكم الخلفي Express API وخدمات بث السجلات SSE.
- `scraper.js`: محرك الزحف واستخراج HTML وتحليل الـ RSS والـ WordPress APIs.
- `db.js`: مدير قاعدة بيانات SQLite وحماية التكرار بنظام Jaccard Similarity.
- `rewriter.js`: محرك إعادة الصياغة الصحفية باستخدام OpenAI API.
- `publisher.js`: بوابة النشر وتصميم قوالب الإسناد ونسب المصدر التلقائي في ووردبريس.
- `runner.js`: المنظم الداخلي لتشغيل خط المعالجة بالكامل.
- `config.json`: ملف الإعدادات الديناميكي النشط لجميع الخوادم والمصادر.
- `public/`: مجلد واجهة لوحة التحكم التفاعلية (HTML, CSS, JavaScript).
- `n8n_workflow.json` & `n8n_guide.md`: ملحقات وأدلة التكامل مع منصة n8n.

---

## 🚀 تشغيل النظام لأول مرة

### 1. تثبيت الحزم والمكتبات الاعتمادية
افتح محطة الأوامر (Terminal/Command Prompt) داخل مجلد `syrian_news_aggregator` ونفذ الأمر التالي:
```bash
npm install
```

### 2. بدء خادم لوحة التحكم التفاعلية
لتشغيل واجهة التحكم التفاعلية على المتصفح:
```bash
npm start
```
بمجرد التشغيل، ستظهر لك رسالة تأكيد تفيد بأن الخادم يعمل بنجاح.
- افتح المتصفح وتوجه إلى الرابط التالي: **`http://localhost:3000`**
- ستشاهد واجهة لوحة التحكم المتكاملة المظلمة.

---

## 🖥️ استخدام لوحة التحكم التفاعلية

تم تقسيم واجهة التحكم إلى 6 تبويبات فائقة السلاسة لتسهيل الإدارة:

### أ. نظرة عامة (Overview)
- يعرض إحصائيات سريعة وحية: إجمالي المقالات المؤرشفة بقاعدة SQLite المحلية، المقالات التي تم نشرها في ووردبريس بنجاح، وعدد المصادر النشطة.
- يظهر تقريراً تفصيلياً لعدد المقالات المؤرشفة والناجحة لكل مصدر من المصادر الإخبارية.
- يحتوي على ويدجت ذكي يوضح حالة الربط الآني بخادم بوابة ووردبريس وخادم ذكاء OpenAI مع أزرار تشغيل سريعة.

### ب. مصادر الأخبار (News Sources)
- تصفح كامل المصادر الإخبارية البالغ عددها أكثر من 35 موقعاً.
- تفعيل وتعطيل أي مصدر بضغطة زر واحدة (Switch Toggle) لحجبه أو تضمينه في دورة الجلب القادمة.
- إمكانية إضافة مصدر جديد بالكامل، أو تعديل محددات الـ HTML والـ Selectors لأي موقع قائم، أو حذفه نهائياً.

### ج. سجل المقالات (Database Logs)
- يعرض كامل الأخبار التي جلبها النظام وحفظها محلياً.
- يدعم البحث بالكلمة المفتاحية في العناوين والروابط.
- يدعم التصفية حسب مصدر الخبر الإخباري أو حالة ترحيله للووردبريس.
- اضغط على زر **"التفاصيل"** لأي خبر لفتح نافذة منبثقة تعرض نص المقال النهائي المنسق بالكامل، مع تاريخ معالجته، معرّف ووردبريس، ورابط المصدر الأصلي.

### د. إعدادات النظام (System Settings)
- **إعدادات ووردبريس**:
  - اكتب رابط موقعك الرئيسي (مثال: `https://my-news-site.com`).
  - اسم مستخدم المشرف (Admin Username).
  - كلمة مرور التطبيق (Application Password) - *انظر قسم إعدادات الووردبريس بالأسفل لطريقة توليدها*.
  - اختر حالة النشر الافتراضية: مسودة (Draft) -ننصح بها للمراجعة والتدقيق اللغوي- أو نشر فوري (Publish).
  - وضع الفحص والتجريب (Dry Run): تفعيله يتيح لك تشغيل النظام بالكامل وتجربته دون استهلاك أي نشر فعلي في موقعك.
- **إعدادات الذكاء الاصطناعي**:
  - تفعيل أو تعطيل إعادة صياغة الأخبار عبر الذكاء الاصطناعي.
  - تعيين مفتاح OpenAI API Key الخاص بك.
  - اختيار النموذج اللغوي (gpt-4o-mini مستحسن لقوته التحريرية والتكلفة الاقتصادية الفائقة).
  - محرر الموجه التحريري (System Prompt Template): قم بصياغة أسلوب الكتابة الذي ترغب أن يلتزم به الروبوت عند كتابة المقال.
- **الإعدادات العامة**: تغيير مسار قاعدة البيانات وتعيين زمن التأخير (Rate Limit Delay) لضمان حماية خادمك من الحظر.

### هـ. منفذ الأوامر المباشر (Terminal Console)
- شاشة سوداء متوهجة تحاكي واجهات الأنظمة الاحترافية.
- اضغط على **"بدء الدمج وجلب الأخبار الآن"** لمشاهدة جلب المواقع لحظة بلحظة، وفحص التكرار، والربط بالـ AI والناشر المباشر.

---

## ⏱️ الجدولة والتشغيل التلقائي (Cron Jobs)

يمكن تشغيل محرك الدمج بشكل صامت كلياً ومجدول دون الحاجة لفتح المتصفح أو لوحة التحكم، وذلك لضمان عمل الموقع بشكل آلي 100%.

لتشغيل خط الجلب والأرشفة لمرة واحدة من سطر الأوامر:
```bash
node index.js --cron
```

### 1. الجدولة في أنظمة Linux/Unix (Crontab)
افتح ملف الجدولة:
```bash
crontab -e
```
أضف السطر التالي لتشغيل النظام بشكل تلقائي بالكامل كل 15 دقيقة بشكل مستمر:
```text
*/15 * * * * cd /path/to/syrian_news_aggregator && node index.js --cron >> cron_logs.log 2>&1
```
*(ملاحظة: استبدل `/path/to/syrian_news_aggregator` بالمسار الحقيقي للمجلد على خادمك)*.

### 2. الجدولة في أنظمة Windows (Task Scheduler)
1. افتح **مجدول المهام (Task Scheduler)** في ويندوز.
2. اختر **إنشاء مهمة أساسية (Create Basic Task)**.
3. اضبط المشغل (Trigger) للتكرار بشكل دوري كل 15 دقيقة.
4. اضبط الإجراء (Action) على: **بدء برنامج (Start a Program)**.
5. في خانة البرنامج/البرمجة (Program/Script) اكتب: `node`.
6. في خانة إضافة وسيطات (Add Arguments) اكتب: `index.js --cron`.
7. في خانة الدليل الافتراضي (Start in) اكتب المسار الكامل للمجلد `syrian_news_aggregator`.

---

## 🔑 تهيئة حساب بوابة ووردبريس (REST API Authentication)

لكي يتمكن المجمع من النشر التلقائي في موقعك، يجب تهيئة **أوراق اعتماد التطبيق** من لوحة تحكم ووردبريس كالتالي:

1. توجه إلى لوحة تحكم ووردبريس الخاصة بموقعك الإخباري كمسؤول.
2. من القائمة الجانبية اختر: **الأعضاء (Users)** -> **حسابك (Profile)**.
3. انزل لأسفل الصفحة حتى تصل إلى قسم **"كلمات مرور التطبيق" (Application Passwords)**.
4. في خانة "اسم كلمة المرور الجديدة للتطبيق" اكتب اسماً تذكارياً (مثال: `News Aggregator Terminal`).
5. اضغط على زر **"إضافة كلمة مرور جديدة للتطبيق"**.
6. سيقوم ووردبريس بتوليد كلمة مرور مكونة من 24 حرفاً مفصولة بفراغات (مثل: `xxxx xxxx xxxx xxxx xxxx xxxx`).
7. **هام جداً**: انسخ هذه الكلمة فوراً واحفظها في مكان آمن، لأنها لن تظهر لك مرة أخرى.
8. توجه للوحة تحكم مجمع الأخبار -> إعدادات النظام -> الصق رابط موقعك، اسم المشرف، وكلمة مرور التطبيق المكونة من 24 حرفاً مع الفراغات كما هي.
9. اضغط على **"فحص الاتصال الآن"** للتأكد من نجاح الربط البرمجي!

---

## 🗄️ هيكل تفاصيل قاعدة بيانات SQLite (DB Schema)

يستخدم النظام قاعدة بيانات SQLite محلية خفيفة وسريعة لتخزين سجلات الجلب ومكافحة التكرار.
تحتوي القاعدة على جدول أساسي يدعى `processed_articles` بالهيكلية التالية:

```sql
CREATE TABLE IF NOT EXISTS processed_articles (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  source_name TEXT NOT NULL,
  url TEXT UNIQUE NOT NULL,
  title TEXT NOT NULL,
  content TEXT NOT NULL,
  wordpress_post_id INTEGER,
  status TEXT NOT NULL,
  processed_at TEXT NOT NULL
);
```

- يتم فحص الحقل `url` بشكل فوري لمنع التكرار المطلق.
- يتم توليد مصفوفة الكلمات للمقالات الجديدة ومقارنتها عبر محرك **Jaccard Distance** المدمج في `db.js` لضمان عدم تكرار الأخبار المتطابقة المحتوى في حال نشرها بمصادر مختلفة.

---

## 🔗 التكامل المباشر مع n8n بصرياً

إذا كنت تفضل إدارة وتوجيه سير العمل بصرياً بالكامل واستخدام **n8n** كأداة جدولة متكاملة:
1. ستجد ملفاً جاهزاً بالكامل للاستيراد باسم `n8n_workflow.json` داخل المجلد.
2. تصفح دليل الاستيراد والتكامل التفصيلي في ملف `n8n_guide.md` المرفق لتشغيل الـ Workflow وربطه بقاعدة البيانات والووردبريس تلقائياً!

---

**مبارك!** نظامك الإخباري الذكي مهيأ بالكامل للتشغيل بأعلى المعايير البرمجية المتطورة. في حال وجود أي استفسار أو رغبة في إضافة ميزات مخصصة إضافية، يرجى مراجعة ملفات الأكواد البرمجية الموثقة جيداً.
